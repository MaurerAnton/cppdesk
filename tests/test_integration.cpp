#include <gtest/gtest.h>
#include <string>
TEST(Integ, Test0000) {
    auto s = std::to_string(0);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 0);
}

TEST(Integ, Test0001) {
    auto s = std::to_string(1);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 1);
}

TEST(Integ, Test0002) {
    auto s = std::to_string(2);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 2);
}

TEST(Integ, Test0003) {
    auto s = std::to_string(3);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 3);
}

TEST(Integ, Test0004) {
    auto s = std::to_string(4);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 4);
}

TEST(Integ, Test0005) {
    auto s = std::to_string(5);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 5);
}

TEST(Integ, Test0006) {
    auto s = std::to_string(6);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 6);
}

TEST(Integ, Test0007) {
    auto s = std::to_string(7);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 7);
}

TEST(Integ, Test0008) {
    auto s = std::to_string(8);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 8);
}

TEST(Integ, Test0009) {
    auto s = std::to_string(9);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 9);
}

TEST(Integ, Test0010) {
    auto s = std::to_string(10);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 10);
}

TEST(Integ, Test0011) {
    auto s = std::to_string(11);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 11);
}

TEST(Integ, Test0012) {
    auto s = std::to_string(12);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 12);
}

TEST(Integ, Test0013) {
    auto s = std::to_string(13);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 13);
}

TEST(Integ, Test0014) {
    auto s = std::to_string(14);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 14);
}

TEST(Integ, Test0015) {
    auto s = std::to_string(15);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 15);
}

TEST(Integ, Test0016) {
    auto s = std::to_string(16);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 16);
}

TEST(Integ, Test0017) {
    auto s = std::to_string(17);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 17);
}

TEST(Integ, Test0018) {
    auto s = std::to_string(18);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 18);
}

TEST(Integ, Test0019) {
    auto s = std::to_string(19);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 19);
}

TEST(Integ, Test0020) {
    auto s = std::to_string(20);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 20);
}

TEST(Integ, Test0021) {
    auto s = std::to_string(21);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 21);
}

TEST(Integ, Test0022) {
    auto s = std::to_string(22);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 22);
}

TEST(Integ, Test0023) {
    auto s = std::to_string(23);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 23);
}

TEST(Integ, Test0024) {
    auto s = std::to_string(24);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 24);
}

TEST(Integ, Test0025) {
    auto s = std::to_string(25);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 25);
}

TEST(Integ, Test0026) {
    auto s = std::to_string(26);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 26);
}

TEST(Integ, Test0027) {
    auto s = std::to_string(27);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 27);
}

TEST(Integ, Test0028) {
    auto s = std::to_string(28);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 28);
}

TEST(Integ, Test0029) {
    auto s = std::to_string(29);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 29);
}

TEST(Integ, Test0030) {
    auto s = std::to_string(30);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 30);
}

TEST(Integ, Test0031) {
    auto s = std::to_string(31);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 31);
}

TEST(Integ, Test0032) {
    auto s = std::to_string(32);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 32);
}

TEST(Integ, Test0033) {
    auto s = std::to_string(33);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 33);
}

TEST(Integ, Test0034) {
    auto s = std::to_string(34);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 34);
}

TEST(Integ, Test0035) {
    auto s = std::to_string(35);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 35);
}

TEST(Integ, Test0036) {
    auto s = std::to_string(36);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 36);
}

TEST(Integ, Test0037) {
    auto s = std::to_string(37);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 37);
}

TEST(Integ, Test0038) {
    auto s = std::to_string(38);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 38);
}

TEST(Integ, Test0039) {
    auto s = std::to_string(39);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 39);
}

TEST(Integ, Test0040) {
    auto s = std::to_string(40);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 40);
}

TEST(Integ, Test0041) {
    auto s = std::to_string(41);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 41);
}

TEST(Integ, Test0042) {
    auto s = std::to_string(42);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 42);
}

TEST(Integ, Test0043) {
    auto s = std::to_string(43);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 43);
}

TEST(Integ, Test0044) {
    auto s = std::to_string(44);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 44);
}

TEST(Integ, Test0045) {
    auto s = std::to_string(45);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 45);
}

TEST(Integ, Test0046) {
    auto s = std::to_string(46);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 46);
}

TEST(Integ, Test0047) {
    auto s = std::to_string(47);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 47);
}

TEST(Integ, Test0048) {
    auto s = std::to_string(48);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 48);
}

TEST(Integ, Test0049) {
    auto s = std::to_string(49);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 49);
}

TEST(Integ, Test0050) {
    auto s = std::to_string(50);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 50);
}

TEST(Integ, Test0051) {
    auto s = std::to_string(51);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 51);
}

TEST(Integ, Test0052) {
    auto s = std::to_string(52);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 52);
}

TEST(Integ, Test0053) {
    auto s = std::to_string(53);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 53);
}

TEST(Integ, Test0054) {
    auto s = std::to_string(54);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 54);
}

TEST(Integ, Test0055) {
    auto s = std::to_string(55);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 55);
}

TEST(Integ, Test0056) {
    auto s = std::to_string(56);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 56);
}

TEST(Integ, Test0057) {
    auto s = std::to_string(57);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 57);
}

TEST(Integ, Test0058) {
    auto s = std::to_string(58);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 58);
}

TEST(Integ, Test0059) {
    auto s = std::to_string(59);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 59);
}

TEST(Integ, Test0060) {
    auto s = std::to_string(60);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 60);
}

TEST(Integ, Test0061) {
    auto s = std::to_string(61);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 61);
}

TEST(Integ, Test0062) {
    auto s = std::to_string(62);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 62);
}

TEST(Integ, Test0063) {
    auto s = std::to_string(63);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 63);
}

TEST(Integ, Test0064) {
    auto s = std::to_string(64);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 64);
}

TEST(Integ, Test0065) {
    auto s = std::to_string(65);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 65);
}

TEST(Integ, Test0066) {
    auto s = std::to_string(66);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 66);
}

TEST(Integ, Test0067) {
    auto s = std::to_string(67);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 67);
}

TEST(Integ, Test0068) {
    auto s = std::to_string(68);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 68);
}

TEST(Integ, Test0069) {
    auto s = std::to_string(69);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 69);
}

TEST(Integ, Test0070) {
    auto s = std::to_string(70);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 70);
}

TEST(Integ, Test0071) {
    auto s = std::to_string(71);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 71);
}

TEST(Integ, Test0072) {
    auto s = std::to_string(72);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 72);
}

TEST(Integ, Test0073) {
    auto s = std::to_string(73);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 73);
}

TEST(Integ, Test0074) {
    auto s = std::to_string(74);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 74);
}

TEST(Integ, Test0075) {
    auto s = std::to_string(75);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 75);
}

TEST(Integ, Test0076) {
    auto s = std::to_string(76);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 76);
}

TEST(Integ, Test0077) {
    auto s = std::to_string(77);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 77);
}

TEST(Integ, Test0078) {
    auto s = std::to_string(78);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 78);
}

TEST(Integ, Test0079) {
    auto s = std::to_string(79);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 79);
}

TEST(Integ, Test0080) {
    auto s = std::to_string(80);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 80);
}

TEST(Integ, Test0081) {
    auto s = std::to_string(81);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 81);
}

TEST(Integ, Test0082) {
    auto s = std::to_string(82);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 82);
}

TEST(Integ, Test0083) {
    auto s = std::to_string(83);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 83);
}

TEST(Integ, Test0084) {
    auto s = std::to_string(84);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 84);
}

TEST(Integ, Test0085) {
    auto s = std::to_string(85);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 85);
}

TEST(Integ, Test0086) {
    auto s = std::to_string(86);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 86);
}

TEST(Integ, Test0087) {
    auto s = std::to_string(87);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 87);
}

TEST(Integ, Test0088) {
    auto s = std::to_string(88);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 88);
}

TEST(Integ, Test0089) {
    auto s = std::to_string(89);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 89);
}

TEST(Integ, Test0090) {
    auto s = std::to_string(90);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 90);
}

TEST(Integ, Test0091) {
    auto s = std::to_string(91);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 91);
}

TEST(Integ, Test0092) {
    auto s = std::to_string(92);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 92);
}

TEST(Integ, Test0093) {
    auto s = std::to_string(93);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 93);
}

TEST(Integ, Test0094) {
    auto s = std::to_string(94);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 94);
}

TEST(Integ, Test0095) {
    auto s = std::to_string(95);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 95);
}

TEST(Integ, Test0096) {
    auto s = std::to_string(96);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 96);
}

TEST(Integ, Test0097) {
    auto s = std::to_string(97);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 97);
}

TEST(Integ, Test0098) {
    auto s = std::to_string(98);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 98);
}

TEST(Integ, Test0099) {
    auto s = std::to_string(99);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 99);
}

TEST(Integ, Test0100) {
    auto s = std::to_string(100);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 100);
}

TEST(Integ, Test0101) {
    auto s = std::to_string(101);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 101);
}

TEST(Integ, Test0102) {
    auto s = std::to_string(102);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 102);
}

TEST(Integ, Test0103) {
    auto s = std::to_string(103);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 103);
}

TEST(Integ, Test0104) {
    auto s = std::to_string(104);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 104);
}

TEST(Integ, Test0105) {
    auto s = std::to_string(105);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 105);
}

TEST(Integ, Test0106) {
    auto s = std::to_string(106);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 106);
}

TEST(Integ, Test0107) {
    auto s = std::to_string(107);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 107);
}

TEST(Integ, Test0108) {
    auto s = std::to_string(108);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 108);
}

TEST(Integ, Test0109) {
    auto s = std::to_string(109);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 109);
}

TEST(Integ, Test0110) {
    auto s = std::to_string(110);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 110);
}

TEST(Integ, Test0111) {
    auto s = std::to_string(111);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 111);
}

TEST(Integ, Test0112) {
    auto s = std::to_string(112);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 112);
}

TEST(Integ, Test0113) {
    auto s = std::to_string(113);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 113);
}

TEST(Integ, Test0114) {
    auto s = std::to_string(114);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 114);
}

TEST(Integ, Test0115) {
    auto s = std::to_string(115);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 115);
}

TEST(Integ, Test0116) {
    auto s = std::to_string(116);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 116);
}

TEST(Integ, Test0117) {
    auto s = std::to_string(117);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 117);
}

TEST(Integ, Test0118) {
    auto s = std::to_string(118);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 118);
}

TEST(Integ, Test0119) {
    auto s = std::to_string(119);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 119);
}

TEST(Integ, Test0120) {
    auto s = std::to_string(120);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 120);
}

TEST(Integ, Test0121) {
    auto s = std::to_string(121);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 121);
}

TEST(Integ, Test0122) {
    auto s = std::to_string(122);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 122);
}

TEST(Integ, Test0123) {
    auto s = std::to_string(123);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 123);
}

TEST(Integ, Test0124) {
    auto s = std::to_string(124);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 124);
}

TEST(Integ, Test0125) {
    auto s = std::to_string(125);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 125);
}

TEST(Integ, Test0126) {
    auto s = std::to_string(126);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 126);
}

TEST(Integ, Test0127) {
    auto s = std::to_string(127);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 127);
}

TEST(Integ, Test0128) {
    auto s = std::to_string(128);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 128);
}

TEST(Integ, Test0129) {
    auto s = std::to_string(129);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 129);
}

TEST(Integ, Test0130) {
    auto s = std::to_string(130);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 130);
}

TEST(Integ, Test0131) {
    auto s = std::to_string(131);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 131);
}

TEST(Integ, Test0132) {
    auto s = std::to_string(132);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 132);
}

TEST(Integ, Test0133) {
    auto s = std::to_string(133);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 133);
}

TEST(Integ, Test0134) {
    auto s = std::to_string(134);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 134);
}

TEST(Integ, Test0135) {
    auto s = std::to_string(135);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 135);
}

TEST(Integ, Test0136) {
    auto s = std::to_string(136);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 136);
}

TEST(Integ, Test0137) {
    auto s = std::to_string(137);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 137);
}

TEST(Integ, Test0138) {
    auto s = std::to_string(138);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 138);
}

TEST(Integ, Test0139) {
    auto s = std::to_string(139);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 139);
}

TEST(Integ, Test0140) {
    auto s = std::to_string(140);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 140);
}

TEST(Integ, Test0141) {
    auto s = std::to_string(141);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 141);
}

TEST(Integ, Test0142) {
    auto s = std::to_string(142);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 142);
}

TEST(Integ, Test0143) {
    auto s = std::to_string(143);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 143);
}

TEST(Integ, Test0144) {
    auto s = std::to_string(144);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 144);
}

TEST(Integ, Test0145) {
    auto s = std::to_string(145);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 145);
}

TEST(Integ, Test0146) {
    auto s = std::to_string(146);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 146);
}

TEST(Integ, Test0147) {
    auto s = std::to_string(147);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 147);
}

TEST(Integ, Test0148) {
    auto s = std::to_string(148);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 148);
}

TEST(Integ, Test0149) {
    auto s = std::to_string(149);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 149);
}

TEST(Integ, Test0150) {
    auto s = std::to_string(150);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 150);
}

TEST(Integ, Test0151) {
    auto s = std::to_string(151);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 151);
}

TEST(Integ, Test0152) {
    auto s = std::to_string(152);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 152);
}

TEST(Integ, Test0153) {
    auto s = std::to_string(153);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 153);
}

TEST(Integ, Test0154) {
    auto s = std::to_string(154);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 154);
}

TEST(Integ, Test0155) {
    auto s = std::to_string(155);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 155);
}

TEST(Integ, Test0156) {
    auto s = std::to_string(156);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 156);
}

TEST(Integ, Test0157) {
    auto s = std::to_string(157);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 157);
}

TEST(Integ, Test0158) {
    auto s = std::to_string(158);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 158);
}

TEST(Integ, Test0159) {
    auto s = std::to_string(159);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 159);
}

TEST(Integ, Test0160) {
    auto s = std::to_string(160);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 160);
}

TEST(Integ, Test0161) {
    auto s = std::to_string(161);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 161);
}

TEST(Integ, Test0162) {
    auto s = std::to_string(162);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 162);
}

TEST(Integ, Test0163) {
    auto s = std::to_string(163);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 163);
}

TEST(Integ, Test0164) {
    auto s = std::to_string(164);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 164);
}

TEST(Integ, Test0165) {
    auto s = std::to_string(165);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 165);
}

TEST(Integ, Test0166) {
    auto s = std::to_string(166);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 166);
}

TEST(Integ, Test0167) {
    auto s = std::to_string(167);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 167);
}

TEST(Integ, Test0168) {
    auto s = std::to_string(168);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 168);
}

TEST(Integ, Test0169) {
    auto s = std::to_string(169);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 169);
}

TEST(Integ, Test0170) {
    auto s = std::to_string(170);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 170);
}

TEST(Integ, Test0171) {
    auto s = std::to_string(171);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 171);
}

TEST(Integ, Test0172) {
    auto s = std::to_string(172);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 172);
}

TEST(Integ, Test0173) {
    auto s = std::to_string(173);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 173);
}

TEST(Integ, Test0174) {
    auto s = std::to_string(174);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 174);
}

TEST(Integ, Test0175) {
    auto s = std::to_string(175);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 175);
}

TEST(Integ, Test0176) {
    auto s = std::to_string(176);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 176);
}

TEST(Integ, Test0177) {
    auto s = std::to_string(177);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 177);
}

TEST(Integ, Test0178) {
    auto s = std::to_string(178);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 178);
}

TEST(Integ, Test0179) {
    auto s = std::to_string(179);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 179);
}

TEST(Integ, Test0180) {
    auto s = std::to_string(180);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 180);
}

TEST(Integ, Test0181) {
    auto s = std::to_string(181);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 181);
}

TEST(Integ, Test0182) {
    auto s = std::to_string(182);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 182);
}

TEST(Integ, Test0183) {
    auto s = std::to_string(183);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 183);
}

TEST(Integ, Test0184) {
    auto s = std::to_string(184);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 184);
}

TEST(Integ, Test0185) {
    auto s = std::to_string(185);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 185);
}

TEST(Integ, Test0186) {
    auto s = std::to_string(186);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 186);
}

TEST(Integ, Test0187) {
    auto s = std::to_string(187);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 187);
}

TEST(Integ, Test0188) {
    auto s = std::to_string(188);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 188);
}

TEST(Integ, Test0189) {
    auto s = std::to_string(189);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 189);
}

TEST(Integ, Test0190) {
    auto s = std::to_string(190);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 190);
}

TEST(Integ, Test0191) {
    auto s = std::to_string(191);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 191);
}

TEST(Integ, Test0192) {
    auto s = std::to_string(192);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 192);
}

TEST(Integ, Test0193) {
    auto s = std::to_string(193);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 193);
}

TEST(Integ, Test0194) {
    auto s = std::to_string(194);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 194);
}

TEST(Integ, Test0195) {
    auto s = std::to_string(195);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 195);
}

TEST(Integ, Test0196) {
    auto s = std::to_string(196);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 196);
}

TEST(Integ, Test0197) {
    auto s = std::to_string(197);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 197);
}

TEST(Integ, Test0198) {
    auto s = std::to_string(198);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 198);
}

TEST(Integ, Test0199) {
    auto s = std::to_string(199);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 199);
}

TEST(Integ, Test0200) {
    auto s = std::to_string(200);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 200);
}

TEST(Integ, Test0201) {
    auto s = std::to_string(201);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 201);
}

TEST(Integ, Test0202) {
    auto s = std::to_string(202);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 202);
}

TEST(Integ, Test0203) {
    auto s = std::to_string(203);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 203);
}

TEST(Integ, Test0204) {
    auto s = std::to_string(204);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 204);
}

TEST(Integ, Test0205) {
    auto s = std::to_string(205);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 205);
}

TEST(Integ, Test0206) {
    auto s = std::to_string(206);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 206);
}

TEST(Integ, Test0207) {
    auto s = std::to_string(207);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 207);
}

TEST(Integ, Test0208) {
    auto s = std::to_string(208);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 208);
}

TEST(Integ, Test0209) {
    auto s = std::to_string(209);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 209);
}

TEST(Integ, Test0210) {
    auto s = std::to_string(210);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 210);
}

TEST(Integ, Test0211) {
    auto s = std::to_string(211);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 211);
}

TEST(Integ, Test0212) {
    auto s = std::to_string(212);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 212);
}

TEST(Integ, Test0213) {
    auto s = std::to_string(213);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 213);
}

TEST(Integ, Test0214) {
    auto s = std::to_string(214);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 214);
}

TEST(Integ, Test0215) {
    auto s = std::to_string(215);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 215);
}

TEST(Integ, Test0216) {
    auto s = std::to_string(216);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 216);
}

TEST(Integ, Test0217) {
    auto s = std::to_string(217);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 217);
}

TEST(Integ, Test0218) {
    auto s = std::to_string(218);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 218);
}

TEST(Integ, Test0219) {
    auto s = std::to_string(219);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 219);
}

TEST(Integ, Test0220) {
    auto s = std::to_string(220);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 220);
}

TEST(Integ, Test0221) {
    auto s = std::to_string(221);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 221);
}

TEST(Integ, Test0222) {
    auto s = std::to_string(222);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 222);
}

TEST(Integ, Test0223) {
    auto s = std::to_string(223);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 223);
}

TEST(Integ, Test0224) {
    auto s = std::to_string(224);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 224);
}

TEST(Integ, Test0225) {
    auto s = std::to_string(225);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 225);
}

TEST(Integ, Test0226) {
    auto s = std::to_string(226);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 226);
}

TEST(Integ, Test0227) {
    auto s = std::to_string(227);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 227);
}

TEST(Integ, Test0228) {
    auto s = std::to_string(228);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 228);
}

TEST(Integ, Test0229) {
    auto s = std::to_string(229);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 229);
}

TEST(Integ, Test0230) {
    auto s = std::to_string(230);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 230);
}

TEST(Integ, Test0231) {
    auto s = std::to_string(231);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 231);
}

TEST(Integ, Test0232) {
    auto s = std::to_string(232);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 232);
}

TEST(Integ, Test0233) {
    auto s = std::to_string(233);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 233);
}

TEST(Integ, Test0234) {
    auto s = std::to_string(234);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 234);
}

TEST(Integ, Test0235) {
    auto s = std::to_string(235);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 235);
}

TEST(Integ, Test0236) {
    auto s = std::to_string(236);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 236);
}

TEST(Integ, Test0237) {
    auto s = std::to_string(237);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 237);
}

TEST(Integ, Test0238) {
    auto s = std::to_string(238);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 238);
}

TEST(Integ, Test0239) {
    auto s = std::to_string(239);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 239);
}

TEST(Integ, Test0240) {
    auto s = std::to_string(240);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 240);
}

TEST(Integ, Test0241) {
    auto s = std::to_string(241);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 241);
}

TEST(Integ, Test0242) {
    auto s = std::to_string(242);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 242);
}

TEST(Integ, Test0243) {
    auto s = std::to_string(243);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 243);
}

TEST(Integ, Test0244) {
    auto s = std::to_string(244);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 244);
}

TEST(Integ, Test0245) {
    auto s = std::to_string(245);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 245);
}

TEST(Integ, Test0246) {
    auto s = std::to_string(246);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 246);
}

TEST(Integ, Test0247) {
    auto s = std::to_string(247);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 247);
}

TEST(Integ, Test0248) {
    auto s = std::to_string(248);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 248);
}

TEST(Integ, Test0249) {
    auto s = std::to_string(249);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 249);
}

TEST(Integ, Test0250) {
    auto s = std::to_string(250);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 250);
}

TEST(Integ, Test0251) {
    auto s = std::to_string(251);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 251);
}

TEST(Integ, Test0252) {
    auto s = std::to_string(252);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 252);
}

TEST(Integ, Test0253) {
    auto s = std::to_string(253);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 253);
}

TEST(Integ, Test0254) {
    auto s = std::to_string(254);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 254);
}

TEST(Integ, Test0255) {
    auto s = std::to_string(255);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 255);
}

TEST(Integ, Test0256) {
    auto s = std::to_string(256);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 256);
}

TEST(Integ, Test0257) {
    auto s = std::to_string(257);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 257);
}

TEST(Integ, Test0258) {
    auto s = std::to_string(258);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 258);
}

TEST(Integ, Test0259) {
    auto s = std::to_string(259);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 259);
}

TEST(Integ, Test0260) {
    auto s = std::to_string(260);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 260);
}

TEST(Integ, Test0261) {
    auto s = std::to_string(261);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 261);
}

TEST(Integ, Test0262) {
    auto s = std::to_string(262);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 262);
}

TEST(Integ, Test0263) {
    auto s = std::to_string(263);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 263);
}

TEST(Integ, Test0264) {
    auto s = std::to_string(264);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 264);
}

TEST(Integ, Test0265) {
    auto s = std::to_string(265);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 265);
}

TEST(Integ, Test0266) {
    auto s = std::to_string(266);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 266);
}

TEST(Integ, Test0267) {
    auto s = std::to_string(267);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 267);
}

TEST(Integ, Test0268) {
    auto s = std::to_string(268);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 268);
}

TEST(Integ, Test0269) {
    auto s = std::to_string(269);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 269);
}

TEST(Integ, Test0270) {
    auto s = std::to_string(270);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 270);
}

TEST(Integ, Test0271) {
    auto s = std::to_string(271);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 271);
}

TEST(Integ, Test0272) {
    auto s = std::to_string(272);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 272);
}

TEST(Integ, Test0273) {
    auto s = std::to_string(273);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 273);
}

TEST(Integ, Test0274) {
    auto s = std::to_string(274);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 274);
}

TEST(Integ, Test0275) {
    auto s = std::to_string(275);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 275);
}

TEST(Integ, Test0276) {
    auto s = std::to_string(276);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 276);
}

TEST(Integ, Test0277) {
    auto s = std::to_string(277);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 277);
}

TEST(Integ, Test0278) {
    auto s = std::to_string(278);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 278);
}

TEST(Integ, Test0279) {
    auto s = std::to_string(279);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 279);
}

TEST(Integ, Test0280) {
    auto s = std::to_string(280);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 280);
}

TEST(Integ, Test0281) {
    auto s = std::to_string(281);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 281);
}

TEST(Integ, Test0282) {
    auto s = std::to_string(282);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 282);
}

TEST(Integ, Test0283) {
    auto s = std::to_string(283);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 283);
}

TEST(Integ, Test0284) {
    auto s = std::to_string(284);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 284);
}

TEST(Integ, Test0285) {
    auto s = std::to_string(285);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 285);
}

TEST(Integ, Test0286) {
    auto s = std::to_string(286);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 286);
}

TEST(Integ, Test0287) {
    auto s = std::to_string(287);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 287);
}

TEST(Integ, Test0288) {
    auto s = std::to_string(288);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 288);
}

TEST(Integ, Test0289) {
    auto s = std::to_string(289);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 289);
}

TEST(Integ, Test0290) {
    auto s = std::to_string(290);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 290);
}

TEST(Integ, Test0291) {
    auto s = std::to_string(291);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 291);
}

TEST(Integ, Test0292) {
    auto s = std::to_string(292);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 292);
}

TEST(Integ, Test0293) {
    auto s = std::to_string(293);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 293);
}

TEST(Integ, Test0294) {
    auto s = std::to_string(294);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 294);
}

TEST(Integ, Test0295) {
    auto s = std::to_string(295);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 295);
}

TEST(Integ, Test0296) {
    auto s = std::to_string(296);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 296);
}

TEST(Integ, Test0297) {
    auto s = std::to_string(297);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 297);
}

TEST(Integ, Test0298) {
    auto s = std::to_string(298);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 298);
}

TEST(Integ, Test0299) {
    auto s = std::to_string(299);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 299);
}

TEST(Integ, Test0300) {
    auto s = std::to_string(300);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 300);
}

TEST(Integ, Test0301) {
    auto s = std::to_string(301);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 301);
}

TEST(Integ, Test0302) {
    auto s = std::to_string(302);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 302);
}

TEST(Integ, Test0303) {
    auto s = std::to_string(303);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 303);
}

TEST(Integ, Test0304) {
    auto s = std::to_string(304);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 304);
}

TEST(Integ, Test0305) {
    auto s = std::to_string(305);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 305);
}

TEST(Integ, Test0306) {
    auto s = std::to_string(306);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 306);
}

TEST(Integ, Test0307) {
    auto s = std::to_string(307);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 307);
}

TEST(Integ, Test0308) {
    auto s = std::to_string(308);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 308);
}

TEST(Integ, Test0309) {
    auto s = std::to_string(309);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 309);
}

TEST(Integ, Test0310) {
    auto s = std::to_string(310);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 310);
}

TEST(Integ, Test0311) {
    auto s = std::to_string(311);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 311);
}

TEST(Integ, Test0312) {
    auto s = std::to_string(312);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 312);
}

TEST(Integ, Test0313) {
    auto s = std::to_string(313);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 313);
}

TEST(Integ, Test0314) {
    auto s = std::to_string(314);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 314);
}

TEST(Integ, Test0315) {
    auto s = std::to_string(315);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 315);
}

TEST(Integ, Test0316) {
    auto s = std::to_string(316);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 316);
}

TEST(Integ, Test0317) {
    auto s = std::to_string(317);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 317);
}

TEST(Integ, Test0318) {
    auto s = std::to_string(318);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 318);
}

TEST(Integ, Test0319) {
    auto s = std::to_string(319);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 319);
}

TEST(Integ, Test0320) {
    auto s = std::to_string(320);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 320);
}

TEST(Integ, Test0321) {
    auto s = std::to_string(321);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 321);
}

TEST(Integ, Test0322) {
    auto s = std::to_string(322);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 322);
}

TEST(Integ, Test0323) {
    auto s = std::to_string(323);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 323);
}

TEST(Integ, Test0324) {
    auto s = std::to_string(324);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 324);
}

TEST(Integ, Test0325) {
    auto s = std::to_string(325);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 325);
}

TEST(Integ, Test0326) {
    auto s = std::to_string(326);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 326);
}

TEST(Integ, Test0327) {
    auto s = std::to_string(327);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 327);
}

TEST(Integ, Test0328) {
    auto s = std::to_string(328);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 328);
}

TEST(Integ, Test0329) {
    auto s = std::to_string(329);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 329);
}

TEST(Integ, Test0330) {
    auto s = std::to_string(330);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 330);
}

TEST(Integ, Test0331) {
    auto s = std::to_string(331);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 331);
}

TEST(Integ, Test0332) {
    auto s = std::to_string(332);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 332);
}

TEST(Integ, Test0333) {
    auto s = std::to_string(333);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 333);
}

TEST(Integ, Test0334) {
    auto s = std::to_string(334);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 334);
}

TEST(Integ, Test0335) {
    auto s = std::to_string(335);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 335);
}

TEST(Integ, Test0336) {
    auto s = std::to_string(336);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 336);
}

TEST(Integ, Test0337) {
    auto s = std::to_string(337);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 337);
}

TEST(Integ, Test0338) {
    auto s = std::to_string(338);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 338);
}

TEST(Integ, Test0339) {
    auto s = std::to_string(339);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 339);
}

TEST(Integ, Test0340) {
    auto s = std::to_string(340);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 340);
}

TEST(Integ, Test0341) {
    auto s = std::to_string(341);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 341);
}

TEST(Integ, Test0342) {
    auto s = std::to_string(342);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 342);
}

TEST(Integ, Test0343) {
    auto s = std::to_string(343);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 343);
}

TEST(Integ, Test0344) {
    auto s = std::to_string(344);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 344);
}

TEST(Integ, Test0345) {
    auto s = std::to_string(345);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 345);
}

TEST(Integ, Test0346) {
    auto s = std::to_string(346);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 346);
}

TEST(Integ, Test0347) {
    auto s = std::to_string(347);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 347);
}

TEST(Integ, Test0348) {
    auto s = std::to_string(348);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 348);
}

TEST(Integ, Test0349) {
    auto s = std::to_string(349);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 349);
}

TEST(Integ, Test0350) {
    auto s = std::to_string(350);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 350);
}

TEST(Integ, Test0351) {
    auto s = std::to_string(351);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 351);
}

TEST(Integ, Test0352) {
    auto s = std::to_string(352);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 352);
}

TEST(Integ, Test0353) {
    auto s = std::to_string(353);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 353);
}

TEST(Integ, Test0354) {
    auto s = std::to_string(354);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 354);
}

TEST(Integ, Test0355) {
    auto s = std::to_string(355);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 355);
}

TEST(Integ, Test0356) {
    auto s = std::to_string(356);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 356);
}

TEST(Integ, Test0357) {
    auto s = std::to_string(357);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 357);
}

TEST(Integ, Test0358) {
    auto s = std::to_string(358);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 358);
}

TEST(Integ, Test0359) {
    auto s = std::to_string(359);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 359);
}

TEST(Integ, Test0360) {
    auto s = std::to_string(360);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 360);
}

TEST(Integ, Test0361) {
    auto s = std::to_string(361);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 361);
}

TEST(Integ, Test0362) {
    auto s = std::to_string(362);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 362);
}

TEST(Integ, Test0363) {
    auto s = std::to_string(363);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 363);
}

TEST(Integ, Test0364) {
    auto s = std::to_string(364);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 364);
}

TEST(Integ, Test0365) {
    auto s = std::to_string(365);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 365);
}

TEST(Integ, Test0366) {
    auto s = std::to_string(366);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 366);
}

TEST(Integ, Test0367) {
    auto s = std::to_string(367);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 367);
}

TEST(Integ, Test0368) {
    auto s = std::to_string(368);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 368);
}

TEST(Integ, Test0369) {
    auto s = std::to_string(369);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 369);
}

TEST(Integ, Test0370) {
    auto s = std::to_string(370);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 370);
}

TEST(Integ, Test0371) {
    auto s = std::to_string(371);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 371);
}

TEST(Integ, Test0372) {
    auto s = std::to_string(372);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 372);
}

TEST(Integ, Test0373) {
    auto s = std::to_string(373);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 373);
}

TEST(Integ, Test0374) {
    auto s = std::to_string(374);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 374);
}

TEST(Integ, Test0375) {
    auto s = std::to_string(375);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 375);
}

TEST(Integ, Test0376) {
    auto s = std::to_string(376);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 376);
}

TEST(Integ, Test0377) {
    auto s = std::to_string(377);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 377);
}

TEST(Integ, Test0378) {
    auto s = std::to_string(378);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 378);
}

TEST(Integ, Test0379) {
    auto s = std::to_string(379);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 379);
}

TEST(Integ, Test0380) {
    auto s = std::to_string(380);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 380);
}

TEST(Integ, Test0381) {
    auto s = std::to_string(381);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 381);
}

TEST(Integ, Test0382) {
    auto s = std::to_string(382);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 382);
}

TEST(Integ, Test0383) {
    auto s = std::to_string(383);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 383);
}

TEST(Integ, Test0384) {
    auto s = std::to_string(384);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 384);
}

TEST(Integ, Test0385) {
    auto s = std::to_string(385);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 385);
}

TEST(Integ, Test0386) {
    auto s = std::to_string(386);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 386);
}

TEST(Integ, Test0387) {
    auto s = std::to_string(387);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 387);
}

TEST(Integ, Test0388) {
    auto s = std::to_string(388);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 388);
}

TEST(Integ, Test0389) {
    auto s = std::to_string(389);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 389);
}

TEST(Integ, Test0390) {
    auto s = std::to_string(390);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 390);
}

TEST(Integ, Test0391) {
    auto s = std::to_string(391);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 391);
}

TEST(Integ, Test0392) {
    auto s = std::to_string(392);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 392);
}

TEST(Integ, Test0393) {
    auto s = std::to_string(393);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 393);
}

TEST(Integ, Test0394) {
    auto s = std::to_string(394);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 394);
}

TEST(Integ, Test0395) {
    auto s = std::to_string(395);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 395);
}

TEST(Integ, Test0396) {
    auto s = std::to_string(396);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 396);
}

TEST(Integ, Test0397) {
    auto s = std::to_string(397);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 397);
}

TEST(Integ, Test0398) {
    auto s = std::to_string(398);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 398);
}

TEST(Integ, Test0399) {
    auto s = std::to_string(399);
    EXPECT_FALSE(s.empty());
    int v = std::stoi(s);
    EXPECT_EQ(v, 399);
}

int main(int a, char** v) { ::testing::InitGoogleTest(&a, v); return RUN_ALL_TESTS(); }